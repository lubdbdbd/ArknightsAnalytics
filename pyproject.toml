[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "arknights-analytics"
version = "0.1.0"
description = "Arknights IP merchandise selection and product operations analytics platform"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "ConstantineChenn"}]
dependencies = [
  "numpy>=2.0,<3",
  "pandas>=2.2,<3",
  "requests>=2.32,<3",
  "beautifulsoup4>=4.13,<5",
  "matplotlib>=3.9,<4",
  "openpyxl>=3.1,<4",
  "tabulate>=0.9,<1"
]

[project.optional-dependencies]
test = ["pytest>=8.3,<9", "pytest-cov>=5,<7"]
platform = ["fastapi>=0.115,<1", "uvicorn>=0.30,<1", "redis>=5,<7", "prometheus-client>=0.21,<1", "httpx>=0.27,<1"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q --disable-warnings --maxfail=1"
